---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: chatsessions.smoothoperator.io
spec:
  group: smoothoperator.io
  names:
    kind: ChatSession
    listKind: ChatSessionList
    plural: chatsessions
    singular: chatsession
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ChatSession is the Schema for the chatsessions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ChatSession
            properties:
              charts:
                description: Charts are optional context charts/graphs/images attached
                  by the user
                items:
                  description: ChartRef defines a reference to a chart (image, diagram,
                    metrics graph)
                  properties:
                    blobBase64:
                      description: BlobBase64 is the base64-encoded content of the
                        chart (for inline charts/images)
                      type: string
                    name:
                      description: Name is a descriptive name for the chart
                      minLength: 1
                      type: string
                    url:
                      description: URL is an external URL to the chart resource (S3,
                        HTTP, etc.)
                      type: string
                  required:
                  - name
                  type: object
                type: array
              createdByUI:
                default: true
                description: CreatedByUI is a flag to indicate this CRD was created
                  by the Chat UI
                type: boolean
              metadata:
                description: Metadata contains Git repository information and other
                  metadata
                properties:
                  gitPath:
                    description: GitPath is the subdirectory path within the repo
                      for the app/chart
                    minLength: 1
                    type: string
                  gitRepo:
                    description: GitRepo is the SSH or HTTPS URL of the Git repository
                    minLength: 1
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels are arbitrary key-value pairs for categorization
                    type: object
                required:
                - gitPath
                - gitRepo
                type: object
              preferAuto:
                default: false
                description: PreferAuto indicates whether the user wants automatic
                  application of changes (vs. suggest mode)
                type: boolean
              prompt:
                description: Prompt is the natural language instruction from the user
                minLength: 1
                type: string
              targetNamespace:
                description: TargetNamespace is the Kubernetes namespace where resources
                  will be deployed/managed
                minLength: 1
                type: string
              user:
                description: User is the requester identity/email who initiated this
                  chat session
                minLength: 1
                type: string
            required:
            - metadata
            - prompt
            - targetNamespace
            - user
            type: object
          status:
            description: status defines the observed state of ChatSession
            properties:
              conditions:
                description: Conditions represent the current state of the ChatSession
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastUpdated:
                description: LastUpdated is the timestamp of the last status update
                  (RFC3339)
                type: string
              reason:
                description: Reason provides additional context about the current
                  state
                type: string
              state:
                description: State represents the current processing state of the
                  chat session
                enum:
                - Pending
                - Processing
                - Blocked
                - Completed
                - Failed
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
