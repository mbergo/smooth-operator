{{- if .Values.metrics.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "smooth-operator.fullname" . }}-metrics
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "smooth-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metrics.type }}
  ports:
    - name: metrics
      port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "smooth-operator.selectorLabels" . | nindent 4 }}
{{- end }}
